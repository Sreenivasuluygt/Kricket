<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:typeface">serif</item>
    </style>

    <style name="login_edt" parent="AppTheme">
        <item name="android:layout_marginLeft">@dimen/dp_35</item>
        <item name="android:layout_marginRight">@dimen/dp_35</item>
        <item name="android:background">@color/white</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:minHeight">@dimen/dp_40</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:paddingLeft">@dimen/dp_5</item>
    </style>

    <style name="ball_txtView" parent="AppTheme">
        <item name="android:layout_height">@dimen/dp_35</item>
        <item name="android:layout_width">@dimen/dp_35</item>
        <item name="android:layout_marginTop">@dimen/dp_12</item>
        <item name="android:background">@drawable/ball_background</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/sp_16</item>
    </style>

    <style name="run_txtView" parent="AppTheme">
        <item name="android:layout_height">@dimen/dp_40</item>
        <item name="android:layout_width">@dimen/dp_40</item>
        <item name="android:background">@drawable/create_match</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textSize">@dimen/sp_16</item>
    </style>

    <style name="batsmen_txtView" parent="AppTheme">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:textSize">@dimen/sp_13</item>
        <item name="android:textColor">@color/textHeading</item>
    </style>

</resources>
